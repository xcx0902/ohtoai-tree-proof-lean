       预期难度顺序：J-LK-DH-AFEI-BC-G
       All Closed                                                       
       设答案集合为  ，所有   的交集为  ，则：
                 ，因为   中的元素本来就在所有   中；
                                             。由于每个       都是线性空间，而线性空间
          的交仍是线性空间，因此        必然对异或封闭。
       不妨用线性基维护        ，最后再还原出答案          。记       为   张成的线性空间，即包含   的最
       小异或封闭集合。
       显然至少有             ，于是首先需要求出             对   贡献了哪些基。为了控制复杂度，分两
       种情况：
          若                     ，则           不可能包含在       的真子空间中，因此它
          张成的空间就是整个         。此时直接将   逐元素插入线性基；
          若                     ，则            ，可以暴力枚举整个          再将其中不
          属于   的元素插入线性基。
       令                                      ，已知        的条件下，
              ，而两个线性空间的并仍为线性空间当且仅当其中一个包含另一个，因此只可能有                   或 
              。
       考虑如下算法。初始        ，过程中线性基可能会扩展，即当发现某个              （此时强制要求 
              ），就将   逐元素插入线性基，容易证明停止扩展时             是唯一且最小的。显然   最多扩展 
           次，如果每次扩展后重新判断整个   是否为每个           的子空间，复杂度不优；正确做法是预先为每个 
         建立线性基，每当   新增一个基  ，只需判断   能否被各个还未归为“            类”的   的线性基表示，这
       样总时间复杂度为               。
       Bread                                                            
       设凸多边形周长为  。依次绕一周的   个顶点后，凸多边形姿态恢复，只向右平移  ，所以滚动很多轮时，首尾不完
       整的部分只产生有界影响。若一个周期         内稳定扫过的面积为  ，所求极限就是  。
       记顶点   成为支点时在地面上的横坐标为  。从初始接地边开始累加边长，即               ，
              ，最后     。绕   旋转时，顶点   的轨迹是以       为圆心、      为半径的一段圆弧。需要
       求面积的图形的上边界就是这若干段圆弧的上包络线横坐标在           以内的部分。
       这组圆弧每隔   重复。任意两顶点之间的距离不超过边界上较短的一段长度，故             ；计算     时，只需要
       前后把圆弧序列前后都倍长一份，也就是保留圆心横坐标位于                   的圆弧。
       可以证明，有效的圆弧只有以各个支点         为圆心、以             为半径的上半圆。证明分为两步：第
       一步是把这些圆弧补充成完整的上半圆不会改变包络线；第二步是半径小的上半圆会被半径大的完全覆盖故无效。
       通过分治决策单调性或 SMAWK 算法计算出每个圆心对应的最大半径  ，问题转化为计算           个上半圆的上包络
       线下方面积。注意传统的旋转卡壳求凸多边形直径算法用于计算凸多边形每个顶点距离的最远点是错误的，现场部分
       选手因为这个问题而始终没有通过该题。一个五个点的反例：
      把上半圆写成             。令       ，就有            。固定   时，最高的圆等价于对
      应直线的   最大值。按圆心递增加入圆时，直线斜率也递增，可以用单调栈维护上包络。两个相邻圆          、
            的切换位置为：
      若新交点不大于栈顶圆的起始位置，栈顶圆没有获胜区间，直接弹出；否则记录交点并入栈。这样每个圆最多进出栈
      一次。
      计算圆弧下面积可以通过积分手段，也可以通过将圆弧下方割补为一个扇形和两个三角形计算。
      总时间复杂度为        ，空间复杂度为    。
      Cut Tree                                                     
      先按边编号从小到大建立 Kruskal 重构树：原树顶点作为叶子，每加入一条边就把它两侧的连通块合并，并新建一个
      内部节点。这样边   对应的内部节点编号可以设为        ，祖先编号严格大于后代。两个原树顶点   的 LCA 
      就对应它们路径上编号最大的边，因为这正是两点在加边过程中第一次连通的时刻。
      按重构树叶子的顺序记录相邻叶子的 LCA。若两个叶子的序号为      ，则它们的 LCA 是区间   中相邻叶子 LCA 
      的最大值，因此对这个数组建立 ST 表后即可     查询任意 LCA。
      取一个块长  ，先按询问左端点所在的块分组，再保证每组至多有   个询问。对一组询问，记左端点的最小值和最
      大值为   ，右端点最大值为  。把编号在       内的边，以及本组询问中所有编号大于   的待删除边，统称为
      特殊边。特殊边数量为     ，其余可能出现的边构成公共森林。于是每个询问都可以看成：取公共森林中编号不超
      过   的部分，再补入满足       且    的特殊边。
      先考虑只由公共边组成的森林。把原树定根后，按父亲先于孩子的顺序扫描即可求出公共森林的连通块。对其中一个
      连通块，任意两点之间的原树路径都没有被删掉，所以它们在加入公共边时的连通时刻仍然是路径上编号最大的边。
      于是，全局重构树在这个连通块的叶子上的虚树，正好记录了该连通块随   增大时的合并过程。按全局叶序扫描，并
      为每个公共连通块维护一个栈，就能在线性时间内构造这些虚树；每个节点只会入栈、出栈一次。
      特殊边的两个端点称为关键点。对虚树进行压缩时，只保留包含关键点的部分：若一个内部节点的两侧都没有关键点
      就忽略，只有一侧有关键点就沿用这一侧，两侧都有关键点时新建一个压缩节点并合并两边。关键点数为           ，所
      以压缩树的大小也是     。将本组询问按   递增处理，随着   扫描公共重构树的合并事件，并维护公共森林当前的
      最大连通块权值。
      回答某个询问时，先在压缩树上倒序扫描，得到每个关键点当前所在的公共连通块。再把特殊边统一定向为原树中的
      父亲到孩子，并按孩子深度从深到浅处理需要保留的特殊边。若特殊边为         ，直接把子连通块的权值加到父连通
      块上。从深到浅处理保证一条边下面的其他特殊边都已经处理完。公共部分的最大值和这些累加过程中出现的最大值
      的较大者，就是答案；点权为正保证了连通块合并后只会变大。
      一次预处理的复杂度为        。每组构造公共森林和压缩树需要      ，每个询问处理     条特殊边，因此
      总复杂度为：
      取   为相应的根号级块长即可得到          ，空间复杂度为          。
         Divide and Conquer                                                                         
         令叶子数为           。因为每个节点的值等于它下面叶子中的最大值，所以约束                          的意思是：值   要放在   的
         子树里，而比   大的值不能放在里面。
         把根节点的约束固定为             ，然后只保留有约束的节点，并把每个节点连到最近的有约束祖先，忽略中间没有约束
         的节点。下面几种情况直接无解：同一个节点被要求不同的值；父节点的要求值小于子节点；同一个值出现在两个互
         不包含的子树中，因为一个值只会出现在一个叶子上。
         对压缩树中的每个节点，删掉它下面直接有约束的子树，剩下的位置称为这个节点的“自由位置”。自由位置的数量就
         是当前子树的叶子数减去这些子树的叶子数，所有叶子位置恰好被分到一个区域。若节点要求的值是  ，这个区域里
         只能放不超过   的数。对于值  ，找到要求值为   的最下面那个节点；  必须放在它的自由位置中，否则就无法同时
         满足所有关于   的要求。这个区域没有位置时也直接无解。
         最后从   到   依次放数，并维护一个可用位置池。处理到   时，把要求值为   的区域加入位置池；如果   没有被要
         求，就从池中任选一个位置，方案数是池的大小；如果   有要求，就只能从刚找到的最下面节点的区域中选，方案数
         是该区域的大小；放好后删去一个位置。某一步没有可选位置就无解，所有数放完后答案就是这些方案数的乘积。时
         间复杂度为                ，空间复杂度为              。
         Exponent                                                                                   
         当       时，若              ，则                  不可能成立；而                  时由欧拉定理一定存在这
         样的  。所以只有与   互质的   需要计数，其余数的贡献都是  。                      时唯一的数          的阶为  ，答案也是  。
         设         。由中国剩余定理，一个与   互质的数可以唯一地看成分别模各个   的互质数；它模   的阶就是这些
         局部阶的最小公倍数。对每个质因数幂分别处理：
              若   是奇素数，取模   的原根  。令                       ，则所有互质数恰好是                      ，对应的阶
              为        。
              若      ，       时只有一个互质剩余，不需要参数；                 时可以用                 表示，得到一个长度
              为   的参数；         时每个奇数都能唯一写成               ，其中            且            ，两个部分的阶
              取最小公倍数，因此得到长度为   和                的两个参数。
         于是只需收集一列参数长度                   ：每个奇素数幂贡献                 ，  不贡献，  贡献  ，                贡
         献   和     。对每个参数选择指数                    后，就唯一确定了一个候选数，而且第  个局部参数贡献的阶为 
                 。
         把每个   分解为素数幂。固定一个素数  ，对每个满足                                的参数（          表示   在    中的指
         数），只观察指数   的   部分。由于不同素数部分可以由中国剩余定理独立选择，整体阶中   的指数就是各个参数贡
         献的   指数的最大值。若要求这个最大值不超过 ，对于一个含有   的参数，必须有                                      ，所以该参数
         有         种选择。于是只看   部分时，满足条件的选择数为：
         恰好使整体阶中   的指数等于   的选择数为                          。因此所有选择对   部分阶之和的贡献为：
         不同素数的指数部分相互独立，而整体阶是它们的乘积，所以最终答案就是各个   的乘积。
       预处理不超过       的所有素数后，试除分解   以及各个   即可。若用         表示   的不同质因子个数，则每组
       数据至多需要分解          个不超过   的数，试除部分的复杂度为                  。
       Folding Game of Ohto Ai                                               
       可以证明无论怎么操作，操作次数一定相同。证明方法考虑，虽然不同的操作得到的树可能不同构，操作后形态不同
       的子树一定会在影响答案前变成形态相同的子树。
       但是暴力操作的时间复杂度是          的。考虑如果当前直径有奇数条边，则一次操作会将直径长度减少；如果当前
       直径有偶数条边，则一定存在一个中心点，这个中心点有一些深度为直径一半的子树，一定会把这些子树合并到同一
       个子树内，才会使得当前直径变短，所以可以一轮操作同时合并所有以中心点为根，深度为直径一半的子树。
       由于一次操作会减少的点的数量跟直径长度同阶，所以整个操作流程不同的直径长度数量是                    级别的。对于每
       个相同的直径一轮操作就会使得直径变短，一轮操作的复杂度是              ，综合复杂度为         。
       没有特意卡             ，瓶颈在于排序去重或者并查集。
       Bonus：本题有一个更深入的性质是，固定一个初始的直径端点，每次折叠都折向它，则它一直都会是直径端点。然
       后可以用可并堆维护这个折叠过程，复杂度               。
       Ghost of Tsushima                                                     
       更详细的题解
       设选出的区间数量为  ，并令   表示跨过边         的区间数量。把所有左端点从小到大记为              ，右端点
       从小到大记为           。沿环依次观察左端点时，对应右端点也必须保持相同的循环顺序，否则会有两个区间
       的左右端点次序相反，其中一个区间包含另一个。因此配对只能是某个循环位移；又因为恰有   个区间跨过边 
           ，所以只能把   配给下标为      （超过   后循环）的右端点。于是只需要枚举左右端点是否出现，以及  。
       令         分别表示点   是否为左端点、右端点，令          为它们的前缀和，并记                  。
       先把跨过边       的   个区间视为已经打开，从左到右扫描时，每遇到一个左端点就多一个打开的区间，每遇到一个
       右端点就少一个。因此边           的覆盖数是       ，点   的覆盖数是 
            。把这两类数交错排成序列  ，即                  、         。按照上面的唯一配对，所有前缀
       中打开的区间数均在        内，恰好等价于每个左端点都和正确一侧的右端点配对，所以区间集合合法当且仅当所有 
            都在     内。
       固定   时，左端点和右端点各自选出   个位置，因此            一共有     种。
       再定义         、             。由                        可得相邻两步分别为 
                      和                  ，所以每一步都是   或  ；又因为左右端点总数相同，  
       走   步后回到起点。反过来，两类步长的正负也能唯一还原            ，所以端点序列与这种闭合格路一一对应。把   
       整体上移   后，覆盖次数限制就变成一条不能越过上下边界的格路限制。
       简要说明这里使用的反射原理。设两条禁线之间的距离为  ，固定起点高度                    ，走   步回到   且不碰
       禁线的路径数为：
       将一条闭合路径的第一步循环移到末尾，会把起点高度的奇偶性翻转，这是合法路径间的双射，因此只对所需奇偶性
       的起点求和，结果是所有                的一半。记            ，对所有   求和时，第二项恰好遍历除下
       标模   与   同余以外的所有二项式系数，其和为          ，故所求路径总数为：
       这里的求和实际上只有有限个二项式系数非零。对于边覆盖，限制                       等价于整条格路满足 
                 ，也就是避开高度   和        两条禁线，其间距为           ，所以初步计数为：
       对于点覆盖，限制              等价于                ，两条禁线的间距为           ，所以初步计
       数为：
       当      时，覆盖次数限制已经蕴含合法性所需的上界  ，初步计数没有多算；只需修正                的情况。固定 
             后，合法条件给出                     ，所以合法的   有                      个。
       点覆盖的初步计数把右端放宽为            ，会计入                      个  ，因此每个端点序列恰好
       多算      次，总共需要减去                。
       边覆盖的初步计数使用的是   的范围，因而计入了                          个  ，减去实际合法数量后，每
       个端点序列多算                                 次。因为        ，有           。再
       把右端点序列循环右移一格，记为                             ，对应的前缀差为  ，则 
            。又因为   的最大值一定能在某个   处取得，所以                        。循环平移是所有右端
       点序列上的双射，因此对全部端点序列求和后，                    的贡献相消，只剩下         。因此边覆盖的
       总多算次数为：
       从初步计数中减去这些重复项，得到两类答案（约定下标越界的二项式系数为  ）：
       预处理阶乘和逆阶乘即可        计算二项式系数，并用前缀和递推维护关于   的两类和。公式中的除法均在模 
               下用逆元实现。对固定的  ，第二个求和只有            项非零，因此枚举全部            的总时间复
       杂度为        ，空间复杂度为       。
       Hidden Track (Easy Version)                                           
       更详细的题解
       令        ，第   位直接使用掩码      ，其中        。这样每次询问只观察顶点编号的第   位，所有位
       合起来可以区分所有顶点。
       先对每个   查询一次不翻转任何点的答案，记为  。再查询把顶点   翻转后的答案，记为          。设：
                                          的第 位为
                                          的第 位为
       翻转   只会改变与   相连的边。对一条相邻边      ，若    的第   位相同，这条边会从不跨集合变为跨集合，回答
       增加  ；若这一位不同，回答就减少  。这两种变化恰好都是            ，对   的所有邻边求和便有：
       由于       ，令                        ，就得到   的所有邻居在第   位上的   值之和。
       如果   是端点，它只有一个邻居，所以       只能是   或  ：分别对应邻居这一位为   或  。如果   是内部点，它有
       两个不同的邻居；只要找一位使两个邻居不同，这一位的和就为  。于是当             时，  是端点当且仅当所有      
       都不为  。
       确定一个端点后即可沿路径恢复。若当前点   的前一个点是 pre，那么对每一位都有：
       右侧一定会得到   或  ，从而逐位确定下一个顶点的编号。第一个点没有前驱，直接使用端点规则恢复唯一邻居。
       为了不超过   次询问，先按编号从小到大检查顶点，直到找到第一个端点  。每个已检查顶点的               结果都缓存
       下来，然后从   开始沿路径推导；遇到尚未询问的点再补做   次查询。由于题目保证排列首尾满足              ，按编号
       找到的第一个端点就是应当输出的起点，而另一个端点编号更大，在寻找   时不会被询问。沿路径恢复到已经得到全
       部   个点时直接停止，也不需要最后一个端点的信息。因此只会对           个顶点各查询   次，再加上   次基准查
       询，总次数为              。     时直接输出唯一的排列即可。
       每个顶点的每一位只处理常数次，时间复杂度为          ，空间复杂度为       。
      Island                                                            
      设节点   的子树中最左和最右的叶子分别为        。子树内的叶子在 DFS 序中连续，因此处理完这棵子树后，它只
      可能通过         与外部相连。于是只需记录这三个端点的连通状态：三点全连、只有           相连、只有     
      相连、只有       相连、三点互不相连，共五种；端点重合时先去重即可。
      对每种状态维护方案数，以及这些方案中已经完全离开三个端点的连通块数之和。合并两部分时枚举它们的状态，得
      到合并后三个端点的状态，并统计有多少个连通块在这次合并后不再接触任何端点。由于状态只有五种，可以预先枚
      举所有会用到的合并方式，用一个很小的并查集求出新状态和新产生的连通块数，之后转移时直接查表即可。若新产
      生了   个连通块，则方案数相乘，连通块总数在两边原有贡献之外再加上   乘对应的方案数。
      按照后序遍历做树形 DP。叶子的状态只有它自己；处理非叶节点   时，按照输入顺序从左到右加入儿子。加入一个
      儿子时，需要同时考虑树边        是否保留；从第二个儿子开始，还要考虑前一个儿子的最右叶与当前儿子的最左叶
      之间的链边是否保留。每加入一个儿子，仍只保留当前部分的  、最左叶和最右叶作为端点。处理完根后去掉全部端
      点，剩余连通块全部计入答案。若叶子数为  ，总边数为               ，最后将连通块数之和除以   即可。转移
      次数和每次转移的状态数都是常数，时间复杂度为         。
      也可以利用平面图欧拉公式来做。固定这张图自然的平面嵌入，设保留子图的边数为  ，由基本环围出的有界面数为 
        ，连通块数满足           。其中         ，因此只需计算   的期望。合并两部分或加入一条边时，
      根据端点是否已经连通判断是否新形成一个有界面，并累计基本环的数量。这个做法和上面的 DP 基本相同，同样需
      要记录        三个端点的五种连通状态，并同样可以预处理所有转移。
      Joker or Judger                                                   
      签到题，由于数据范围较小，你可以使用任意你喜欢的方式通过本题。
      一个可行的实现方式是，把所有需要匹配的字符串和对应的分数放进 std::map 里（包括带   的字符串可以拆成   
      个字符串塞进 std::map），然后直接求和与   比较大小。
      另外就像题目提示的那样，你可以直接在样例中复制所有的对应字符串。
      K-MEX                                                             
      我们设原数组   的       为  。我们发现，对于任意给定的  ，一定有           。
      根据     的定义，如果要想让答案超过  ，那么   一定出现在数组里。这样的   至多只有   种可能，分别为 
         ，其中       。
      我们先枚举这样的  。对于一个固定的  ，  只会变成   或者        两种。我们令 
                  。如果数组中没有  ，那么我们就让       ，否则让     。然后我们将答案记忆化。询问的
      时候可以用      或者       单次查询。
      时间复杂度         或者          。
      Loop                                                              
      格子     中的数是       。把原序列循环地向左右展开，向下和向右就分别相当于在序列上向右和向左走一
      步。因此原问题等价于从   出发，向左、向右各走         步，最后回到  。
      可以猜测，存在一种最优走法只走   的某一侧，并在到达最远位置后，不断与前一个位置反复横跳，最后再回到 
        。
      把相邻位置的权值之和记作边的代价，设这一侧从近到远的边代价为             。若最远走到第   条边，前      条
      边各往返一次，剩余的        次往返都在第   条边上，代价就是                 。
      分别从两个方向扫描所有  ，维护边代价的前缀和即可。时间复杂度为   。
      简单证明这个结论。任意合法游走在每条边上向两个方向经过的次数相同。选择其中代价最小的一条边，保留从   
      到这条边的路径各往返一次，再把其余所有往返都移到这条边上。由于选择的是最小代价，答案不会变大，于是它就
      可以作为最远的边，所有多余步数也都在这里反复横跳。
